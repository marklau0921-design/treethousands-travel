import { Link } from 'wouter';
import type { HomepageCtaContent } from '@shared/homepage';
import { trpc } from '@/lib/trpc';
export default function ReadyToStart({content}:{content:HomepageCtaContent}){const {data:homepageAssets}=trpc.media.getHomepageAssets.useQuery();const texture=homepageAssets?.cta?.url||'';const textureOpacity=Math.max(0,Math.min(1,Number(homepageAssets?.cta?.opacity??28)/100));return <section style={{position:'relative',width:'100%',height:'clamp(260px,30vw,275px)',backgroundColor:content.backgroundColor,color:content.textColor,overflow:'hidden',display:'flex',alignItems:'center',justifyContent:'center'}}>{texture&&<div style={{position:'absolute',inset:0,backgroundImage:`url(${texture})`,backgroundSize:'420px 420px',backgroundRepeat:'repeat',opacity:textureOpacity,filter:'contrast(1.45) brightness(1.08)'}}/>}<div style={{position:'relative',zIndex:1,display:'flex',flexDirection:'column',alignItems:'center',gap:28,textAlign:'center',padding:'0 24px'}}><h2 style={{fontFamily:'AlternateGotNo1D',fontSize:'clamp(28px,4vw,48px)',fontWeight:800,color:content.textColor,letterSpacing:'.05em',textTransform:'uppercase',margin:0,lineHeight:1.1}}>{content.title}</h2><Link href={content.buttonHref}><button style={{backgroundColor:content.buttonBackgroundColor,color:content.buttonTextColor,fontFamily:'Lato, sans-serif',fontSize:13,fontWeight:700,letterSpacing:'.15em',textTransform:'uppercase',padding:'14px 36px',border:`2px solid ${content.buttonBackgroundColor}`,cursor:'pointer'}}>{content.buttonLabel}</button></Link></div></section>}
